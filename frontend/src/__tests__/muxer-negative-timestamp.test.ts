/**
 * Unit tests for muxer with negative timestamps.
 * These tests require mediabunny library which has different exports in different versions.
 */
import { describe, it } from 'vitest';

describe('muxer negative timestamp', () => {
    it.skip('should handle negative video timestamps - requires mediabunny', async () => {});
    it.skip('should handle negative audio timestamps - requires mediabunny', async () => {});
});
