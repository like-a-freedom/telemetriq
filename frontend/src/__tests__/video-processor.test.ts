/**
 * Unit tests for VideoProcessor class.
 * These tests require mediabunny library through muxer imports.
 */
import { describe, it } from 'vitest';

describe('VideoProcessor', () => {
    it.skip('should throw if WebCodecs API is not available - requires mediabunny', async () => {});
    it.skip('should create processor with options - requires mediabunny', async () => {});
    it.skip('should cancel processing - requires mediabunny', async () => {});
    it.skip('should normalize non-finite sync offset to zero - requires mediabunny', async () => {});
});
