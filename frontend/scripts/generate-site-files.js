/* eslint-disable no-console */
// generate-site-files.js — NO-OP
// Site files (robots.txt / sitemap.xml) are served dynamically at runtime by the server (Caddy/Vite).
console.warn('generate-site-files.js is a no-op — site files are generated dynamically at runtime');
